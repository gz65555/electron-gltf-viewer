import { styled } from "../../theme/stitches.config";

const StyledPanelContainer = styled("div", {
  padding: "$3 0",
  borderBottom: "1px solid hsla(0,0%,100%,0.05)",
});

export const HeaderContainer = StyledPanelContainer;